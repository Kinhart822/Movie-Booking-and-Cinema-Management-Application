<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Border properties -->
    <solid android:color="@android:color/transparent" /> <!-- Background inside the border -->
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- Border width and color -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- Padding inside the border -->
</shape>
